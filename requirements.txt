# PLC Logger (Python agent) runtime requirements
#
# Minimal API runtime
fastapi>=0.111.0
uvicorn[standard]>=0.27.0
sqlmodel>=0.0.24

# Optional connectors (to be added when implementing)
# pymodbus
# opcua
